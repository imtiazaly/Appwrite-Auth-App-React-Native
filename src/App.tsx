import { View } from 'react-native';
import React from 'react';
import { Routes } from './routes/Router';

const App = () => {
  return (
    <View>
      <Routes />
    </View>
  );
};

export default App;
